class Place {
  String cover;
  String title;
  String subtitle;
  String description;

  Place({
    required this.cover,
    required this.title,
    required this.subtitle,
    required this.description,
  });
}
